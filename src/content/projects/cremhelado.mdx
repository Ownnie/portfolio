---
title:
  es: "CremHelado — Plataforma de innovación con IA"
  en: "CremHelado — AI innovation platform"
slug: "cremhelado"
period:
  es: "2024"
  en: "2024"
role:
  es: "Dev Full-stack (Angular + Spring Boot)"
  en: "Full-stack Dev (Angular + Spring Boot)"
stack: ["Angular","RxJS","Material UI","Spring Boot","REST API","PostgreSQL","Gemini API"]
description:
  es: |
    Módulo de **validaciones con IA** para detectar **propuestas duplicadas** o muy similares y acelerar
    la evaluación. Frontend en **Angular** (formularios reactivos, guards, paginación) y backend **Spring Boot**
    con endpoints REST y jobs de pre-procesamiento. Integración con **Gemini API** para generar embeddings/
    puntuación de similitud y banderas de posibles duplicados. Persistencia en **PostgreSQL** (índices por
    estado/fecha) y colas de validación para no bloquear la UI.
  en: |
    **AI-assisted validation** module to detect **duplicate/near-duplicate** submissions and speed up reviews.
    **Angular** frontend (reactive forms, guards, pagination) and **Spring Boot** backend with REST endpoints
    and preprocessing jobs. **Gemini API** integration for embeddings/similarity scoring with flags for
    potential duplicates. **PostgreSQL** persistence (status/date indexes) and async validation queues to keep the UI responsive.
metrics:
  es: ["-15% redundancia en propuestas"]
  en: ["-15% redundancy in proposals"]
links:
  - { label: "Código", href: "", type: "repo" }
  - { label: "Preview", href: "", type: "demo" }
cover: "/projects/cremhelado-cover.jpg"
featured: true
---
Contribuciones clave:
- Diseño de **pipeline de similitud** (pre-tokenización, embeddings, umbrales y revisión humana).
- Manejo de **re-intentos** y timeouts en llamadas a IA; **circuit breaker** simple en backend.
- Registro de decisiones (audit log) y trazabilidad por propuesta.
