---
title:
  es: "Segundo Proyecto (Calculadora de Propinas y Consumos)"
  en: "Second Project (Tips & Bill Split Calculator)"
slug: "segundo-proyecto-fullstack"
period:
  es: "Fecha desconocida"
  en: "Unknown date"
role:
  es: "Desarrollador Frontend (React + TS)"
  en: "Frontend Developer (React + TS)"
stack: ["React", "TypeScript", "Vite", "Tailwind CSS", "useState", "Custom Hooks"]
description:
  es: |
    Calculadora de cuenta y propina hecha en React + TS. Maneja entradas controladas (total, personas, %),
    valida casos límite (vacío, cero, NaN) y muestra desglose por persona. Refactoricé lógica de cálculo a hooks
    y componentes puros, y apliqué utilidades de formato numérico.
  en: |
    Tips & bill-split calculator built in React + TS. Uses controlled inputs (total, people, tip %),
    validates edge cases (empty, zero, NaN) and shows per-person breakdown. Refactored business logic into hooks
    and pure components; added small number-format utilities.
links:
  - { label: "Código (GitHub)", href: "https://github.com/Ownnie/SegundoProyecto", type: "repo" }
  - { label: "Vista previa", href: "https://segundoproyectofullstack.netlify.app/", type: "demo" }
cover: "/projects/segundo-fullstack-cover.jpg"
featured: false
---
Detalles: manejo de estado con `useState`, derivación de valores (`useMemo`), validación simple,
y utilidades para formateo (`Intl.NumberFormat`).
